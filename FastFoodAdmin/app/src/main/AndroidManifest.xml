<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dcv3.fastfood.fastfoodadmin" >

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="Gokvsi0NGFg7I87lMaRgLjz6XILpfR1AofzzxKOF" />
        //android:value="@string/parse_app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="cmVUZO1VJRrGSRBbc3LmdYluCjxisZKv5nEIx1fl" />
        //android:value="@string/parse_client_key" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    //allows app to access internet
    <uses-permission android:name="android.permission.INTERNET"/>

    //allows app to write to external storage
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    //allows app to access information about networks
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

</manifest>
